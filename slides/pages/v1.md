---
layout: section
---

# v1 — 原生 `<select>` 包裝

打好地基：最小可用、可上線的第一版

---
layout: two-cols-header
---

# v1 · 設計目標

<div class="text-sm opacity-70 -mt-2">用最少的程式碼，換到最穩的行為</div>

::left::

**做了什麼**

- 包裝原生 `<select>`，行為與無障礙**免費繼承**
- `v-model` 雙向綁定（`modelValue` + `update:modelValue`）
- `options` 陣列驅動，支援 `disabled` 單項停用
- `size`：`small` / `medium` / `large`
- `placeholder` 以 disabled hidden option 呈現

::right::

**刻意不做**

- ❌ 自訂下拉面板（沿用瀏覽器原生）
- ❌ 搜尋 / 篩選
- ❌ 多選
- ❌ 自訂 option 樣式

<div class="pt-2 text-sm opacity-60">
→ 這些「不做」正是 v2 之後的升級空間
</div>

---

# v1 · API

| Prop | 型別 | 預設 | 說明 |
| --- | --- | --- | --- |
| `modelValue` | `string \| number \| null` | `null` | 目前選取值（v-model） |
| `options` | `SelectOption[]` | `[]` | 選項清單 |
| `placeholder` | `string` | `"請選擇"` | 未選取時提示 |
| `size` | `"small" \| "medium" \| "large"` | `"medium"` | 尺寸 |
| `disabled` | `boolean` | `false` | 是否整體停用 |

<div class="pt-3 text-sm">

**Emits**：`update:modelValue`、`change` ｜
**型別**：`SelectOption = { label; value; disabled? }`

</div>

---

# v1 · 核心實作

```vue {all|4-8|12-15|3-6}{lines:true}
<script setup lang="ts">
export interface SelectOption {
  label: string;
  value: string | number;
  disabled?: boolean;
}
// props: modelValue / options / placeholder / size / disabled
function onChange(event: Event) {
  const raw = (event.target as HTMLSelectElement).value;
  const matched = options.find((o) => String(o.value) === raw);
  emit("update:modelValue", matched ? matched.value : null);
}
</script>

<template>
  <select :value="modelValue === null ? '' : String(modelValue)" @change="onChange">
    <option value="" disabled hidden>{{ placeholder }}</option>
    <option v-for="o in options" :key="o.value" :value="String(o.value)" :disabled="o.disabled">
      {{ o.label }}
    </option>
  </select>
</template>
```

---
layout: two-cols-header
---

# v1 · 實際跑跑看

<div class="text-sm opacity-70 -mt-2">下方是元件庫裡的真 v1 元件，可以直接操作</div>

::left::

<div class="pt-6 pr-8">

<SelectDemoV1 />

</div>

::right::

<div class="pt-6 text-sm leading-relaxed">

**觀察重點**

- 下拉是**瀏覽器原生**面板（各 OS 長相不同）
- 「榴槤」是 `disabled`，選不到
- 選取後即時回拋值，右側同步更新
- 鍵盤操作、無障礙**開箱即用**

</div>

---
layout: center
---

# v1 · 留下的限制 → v2 的起點

<div class="grid grid-cols-2 gap-6 pt-4 max-w-3xl mx-auto text-left">

<div class="p-4 rounded-lg border border-gray-200">

**🎨 樣式受限**
原生下拉面板無法自訂外觀、無法統一各平台視覺。

</div>

<div class="p-4 rounded-lg border border-gray-200">

**🔍 沒有搜尋**
選項一多就難找，缺少輸入篩選。

</div>

<div class="p-4 rounded-lg border border-gray-200">

**➕ 只能單選**
無法多選、無 tag 呈現。

</div>

<div class="p-4 rounded-lg border border-gray-200">

**🧩 option 內容單一**
只能純文字，放不了圖示 / 副標。

</div>

</div>

<div class="pt-8 text-center text-sm opacity-60">
這些限制，就是下一版升級要解的題目
</div>
