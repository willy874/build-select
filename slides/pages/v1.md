---
layout: section
---

# v1 — 原生 `<select>` 包裝

打好地基：最小可用、可上線的第一版

---

# v1 · 設計目標

<div class="text-sm opacity-70 -mt-2">用最少的程式碼，換到最穩的行為</div>

**做了什麼**

- 包裝原生 `<select>`，行為與無障礙**免費繼承**
- `v-model` 雙向綁定（`modelValue` + `update:modelValue`）
- `options` 陣列驅動，支援 `disabled` 單項停用
- `size`：`small` / `medium` / `large`
- `placeholder` 以 disabled hidden option 呈現

---
layout: two-cols-header
---

# v1 · 實際跑跑看

<div class="text-sm opacity-70 -mt-2">下方是元件庫裡的真 v1 元件，可以直接操作</div>

::left::

<div class="pt-6 pr-8">

<SelectDemoV1 />

</div>

::right::

<div class="pt-6 text-sm leading-relaxed">

**觀察重點**

- 下拉是**瀏覽器原生**面板（各 OS 長相不同）
- 「榴槤」是 `disabled`，選不到
- 選取後即時回拋值，右側同步更新
- 鍵盤操作、無障礙**開箱即用**

</div>
